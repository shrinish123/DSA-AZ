#include<bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'
#define ii pair<int,int> 
#define dbg(x) cout << #x << " = " << x << endl
#define F first
#define S second
#define all(a) a.begin(),a.end()


void solve(){
   
}

// You have given a bracket sequence s1, s2, …, sn or 
// in other words, a string s of length n, consisting of 
// characters '(' and ')'. 
// You've been asked Q queries. The ith query is of the 
// form li and ri. The answer to the i-th query is the length 
// of the maximum correct bracket subsequence of sequence 
// sli, sli + 1, ..., sri.
// Find the correct answer to all queries.



signed main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
     #ifndef ONLINE_JUDGE
     freopen("input.txt", "r", stdin);
     freopen("error.txt", "w", stderr);
     freopen("output.txt", "w", stdout);
     #endif
    solve();
}