#include<bits/stdc++.h>
using namespace std;

#define int long long
#define endl '\n'
#define ii pair<int,int> 
#define dbg(x) cout << #x << " = " << x << endl
#define F first
#define S second
#define all(a) a.begin(),a.end()


void solve(){
   
}

//You have given an array A of n integers. Your task is to answer 
//q queries. Each query contains an integer x. 
//For each query, print the minimum index i such that Ai ≥ x, 
//and then decrease the value at position i by x, 
//i.e., Ai = Ai - x. If there doesn't exist any index i 
//satisfying the condition, then simply print 0.


signed main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
     #ifndef ONLINE_JUDGE
     freopen("input.txt", "r", stdin);
     freopen("error.txt", "w", stderr);
     freopen("output.txt", "w", stdout);
     #endif
    solve();
}