#include<bits/stdc++.h>
using namespace std;

#define int long long 
#define endl '\n'

void solve(){
   string s;
   cin>>s;
   for(int i=0;i<s.length();i++){
      
   }
}

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    string temp;
     int _t;cin>>_t;
    getline(cin,temp);
    while(_t)solve();

}